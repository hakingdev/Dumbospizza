# dependencies
node_modules/

# Next.js
.next/
out/

# production/build output
build/
dist/

# TypeScript
*.tsbuildinfo

# environment files
.env
.env.*
!.env.example
!deploy/env.production.example
deploy/.env.production

# runtime uploads and local backups
public/uploads/
migration-backup/
MIGRATION-BACKUP.md
settings-backup.json
*-backup.json
*.archive.gz
*.tar.gz
*.bak
*.backup
*~

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS/editor/local tool state
.DS_Store
.idea/
.vscode/
.claude/

# deploy / hosting local state
.vercel/
.turbo/
.cache/
coverage/

# secrets / certs / keys
*.pem
*.key
*.p12
*.pfx
auth_info*/
.wwebjs_auth/
.wwebjs_cache/

# screen recordings / heavy media (не нужны в репо)
*.mov
*.mp4

# test / lint caches
coverage/
.vitest/
.eslintcache

# temp / scratch (диагностические скрипты, дампы)
tmp/
temp/
*.tmp
_*.ts
_*.mjs
*.output

# editor local history / process / e2e artefacts (защитные правила)
.history/
*.pid
*.seed
*.pid.lock
playwright-report/
test-results/
.playwright/

# misc
# ВНИМАНИЕ: package-lock.json НЕ игнорируем — он нужен для воспроизводимых
# установок (npm ci) и сборки на Vercel. Раньше тут была опечатка, удалена.
Dockerfile.new
Thumbs.db

# Stripe online payments — отключено (используем SumUp). Код оставлен локально,
# чтобы при желании переключиться обратно; в git не коммитим.
/lib/stripe.ts
/lib/__tests__/stripe.test.ts
/app/api/payments/stripe/
/components/checkout/StripePaymentForm.tsx
.vercel

# Print-agent: локальное persistent-хранилище напечатанных ключей (на машине оператора)
/scripts/printed-orders.json
/scripts/printed-orders.json.tmp
scripts/.backup-2plus1-targetItems-*.json
