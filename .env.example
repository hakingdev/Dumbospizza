NODE_ENV=development
MONGODB_URI=mongodb://mongodb:27017/pizzadelivery

# Supabase / Postgres (Drizzle)
# DATABASE_URL — Supabase → Settings → Database → Connection string (Session pooler, с паролем БД)
DATABASE_URL=postgresql://postgres.PROJECT_REF:DB_PASSWORD@aws-0-REGION.pooler.supabase.com:5432/postgres
# Публичные ключи Supabase (Settings → API)
NEXT_PUBLIC_SUPABASE_URL=https://PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
# Серверный секретный ключ (Settings → API → service_role). НЕ коммитить реальное значение.
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# NextAuth Configuration
NEXTAUTH_SECRET=your_nextauth_secret_key
NEXTAUTH_URL=http://localhost:3000

# Order access tokens — секрет для HMAC-подписи ссылок доступа к заказу/счёту
# (страница подтверждения без сессии). Если не задан — падает на NEXTAUTH_SECRET.
# Сгенерировать: openssl rand -hex 32. Одинаковый на всех инстансах; менять только
# при ротации (инвалидирует уже выданные ссылки на заказы). НЕ коммитить значение.
ORDER_ACCESS_SECRET=your_order_access_secret

# Telegram Bot Integration
TELEGRAM_TOKEN=your_telegram_token
TELEGRAM_CHAT_ID=your_telegram_chat_id

# Stripe Payment Integration
STRIPE_SECRET_KEY=your_stripe_secret_key
STRIPE_PUBLIC_KEY=your_stripe_public_key

# SumUp Online Payments (Apple Pay / Google Pay / card via Payment Widget)
# SUMUP_SECRET_KEY — SumUp Dashboard → Developers → API keys (sup_sk_...). НЕ коммитить реальное значение.
SUMUP_SECRET_KEY=your_sumup_secret_key
# SUMUP_MERCHANT_CODE — merchant_code из GET https://api.sumup.com/v0.1/me (merchant_profile.merchant_code)
SUMUP_MERCHANT_CODE=your_sumup_merchant_code

# Seed Data Configuration
SEED_SECRET_KEY=development_seed_key

# Google Analytics
NEXT_PUBLIC_GA_ID=G-XXXXXXXX

# Meta (Facebook) Conversions API — серверное событие Purchase (lib/conversions/meta-capi-purchase.ts).
# META_PIXEL_ID — ID пикселя (тот же, что в app/layout.tsx). Публичный, не секрет.
# META_CAPI_ACCESS_TOKEN — Events Manager → Datenquellen → пиксель → Einstellungen →
#   Conversions API → «Zugriffsschlüssel generieren». НЕ коммитить реальное значение.
# META_CAPI_TEST_EVENT_CODE — опционально: код из вкладки «Testereignisse» для проверки;
#   в проде оставить пустым, иначе события уходят как тестовые.
# Без META_PIXEL_ID и META_CAPI_ACCESS_TOKEN серверный Purchase молча не отправляется.
META_PIXEL_ID=900934192877252
META_CAPI_ACCESS_TOKEN=your_meta_capi_access_token
META_CAPI_TEST_EVENT_CODE=

# Print agent — секрет должен совпадать с .env принт-агента на офисной машине
PRINT_AGENT_SECRET=your_print_agent_secret

# WhatsApp через Twilio (lib/whatsapp.ts). Если задан TWILIO_ACCOUNT_SID + аутентификация +
# TWILIO_WHATSAPP_FROM — этот режим имеет приоритет над воркером и Meta Cloud API.
# TWILIO_ACCOUNT_SID — console.twilio.com → Account Info (начинается с AC; нужен всегда — входит в URL).
# Аутентификация — одно из двух (API Key приоритетнее, предпочтительный вариант):
#   TWILIO_API_KEY_SID + TWILIO_API_KEY_SECRET — Standard API Key (SK...; secret показывают один раз)
#   TWILIO_AUTH_TOKEN — главный Auth Token из Account Info
# TWILIO_WHATSAPP_FROM — номер сендера, напр. whatsapp:+4930XXXXXXX;
#   для песочницы: whatsapp:+14155238886 (Messaging → Try it out → Send a WhatsApp message).
# TWILIO_CONTENT_SID_* — Content SID (HX...) одобренных шаблонов из Content Template Builder.
#   Пока пустые — шлётся обычный текст: работает в песочнице и в 24h-окне диалога,
#   но НЕ дойдёт до клиента в проде без шаблона. Для прода заполнить оба.
TWILIO_ACCOUNT_SID=
TWILIO_API_KEY_SID=
TWILIO_API_KEY_SECRET=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=
TWILIO_CONTENT_SID_ORDER_PLACED=
TWILIO_CONTENT_SID_ORDER_STATUS=
